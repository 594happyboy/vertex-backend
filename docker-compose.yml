services:
  mysql:
    image: mysql:8.0
    container_name: filedb-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: filedb
      TZ: Asia/Shanghai
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql
      - ./app-bootstrap/src/main/resources/sql/schema.sql:/docker-entrypoint-initdb.d/schema.sql
    command: --default-authentication-plugin=mysql_native_password
    networks:
      - filedb-network

  redis:
    image: redis:7
    container_name: filedb-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    networks:
      - filedb-network

  minio:
    image: minio/minio
    container_name: filedb-minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: admin
      MINIO_ROOT_PASSWORD: admin123
      TZ: Asia/Shanghai
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio-data:/data
    networks:
      - filedb-network

volumes:
  mysql-data:
  redis-data:
  minio-data:

networks:
  filedb-network:
    driver: bridge


